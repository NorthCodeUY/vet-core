# api-backend/requirements.txt

# ==============================================================================
# VET-CORE BACKEND - REQUIREMENTS
# ==============================================================================

# --- Framework Web Principal ---
fastapi==0.139.0            # Framework para construir la API
uvicorn[standard]==0.50.2   # Servidor ASGI de alta velocidad para ejecutar FastAPI

# --- Base de Datos y Persistencia ---
SQLAlchemy==2.0.51          # ORM para interactuar con PostgreSQL
psycopg2-binary==2.9.12     # Driver necesario para que Python hable con PostgreSQL
alembic==1.18.5             # Herramienta para gestionar migraciones

# --- Validación y Configuración --- 
pydantic==2.13.4            # Validación de datos y esquemas
pydantic-settings==2.14.2   # Manejo de variables de entorno (.env)
pydantic_core==2.46.4       # Núcleo de validación de Pydantic
python-dotenv==1.2.2        # Carga automática de variables de entorno
python-multipart==0.0.32    # Necesario para manejar archivos (UploadFile)

# --- Autenticación y Seguridad ---
passlib[bcrypt]==1.7.4      # Hashing seguro de contraseñas (bcrypt)
python-jose[cryptography]==3.5.0 # Creación y verificación de tokens JWT

# --- Procesamiento de Datos (Importación Excel) ---
pandas==3.0.3               # Manipulación y análisis de datos
openpyxl==3.1.5             # Motor necesario para leer archivos .xlsx
scipy==1.18.0               # Utilidades de cálculo científico
numpy==2.5.1                # Base numérica para Pandas

# --- Utilidades y Dependencias del Sistema ---
starlette==1.3.1            # Base de FastAPI
uvloop==0.22.1              # Ciclo de eventos rápido para asyncio
watchfiles==1.2.0           # Permite el --reload automático
websockets==16.0            # Comunicación en tiempo real
PyYAML==6.0.3               # Manejo de archivos YAML
rsa==4.9.1                  # Soporte para criptografía
six==1.17.0                 # Utilidades de compatibilidad
typing-inspection==0.4.2    # Utilidad para Pydantic
typing_extensions==4.16.0   # Extensiones de tipos de Python
python-dateutil==2.9.0.post0 # Utilidades de fechas
annotated-doc==0.0.4        # Documentación de tipos
email-validator>=2.0.0      # Validación de correos electrónicos 