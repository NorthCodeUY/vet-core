# api-backend/requirements.txt

# ==============================================================================
# VET-CORE BACKEND - REQUIREMENTS
# ==============================================================================

# --- Framework Web Principal ---
fastapi                     # Framework para construir la API
uvicorn[standard]           # Servidor ASGI de alta velocidad para ejecutar FastAPI

# --- Base de Datos y Persistencia ---
sqlalchemy                  # ORM para interactuar con PostgreSQL
psycopg2-binary             # Driver necesario para que Python hable con PostgreSQL
alembic                     # Herramienta para gestionar migraciones de la base de datos

# --- Validación y Configuración --- 
pydantic                    # Validación de datos y esquemas (Schemas)
pydantic-settings           # Manejo de variables de entorno (.env)

# --- Autenticación y Seguridad --- <!> con estos tengo que empesar a trabajr haora deberia menejarlo 
passlib[bcrypt]             # Hashing seguro de contraseñas (bcrypt)
python-jose[cryptography]   # Creación y verificación de tokens JWT

# --- Procesamiento de Datos (Importación Excel) ---
pandas                      # Manipulación y análisis de datos de tu planilla
openpyxl                    # Motor necesario para leer archivos .xlsx

# --- Utilidades ---
python-dotenv               # Carga automática de variables de entorno

# <!> esto es lo que tengo ves alguna diferencia y una pregutna no es mejor definirle la version ??  
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-jose==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
rsa==4.9.1
scipy==1.18.0
six==1.17.0
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.50.2
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.0